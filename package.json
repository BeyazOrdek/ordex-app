{
  "name": "ordex-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build && esbuild server.js --bundle --platform=node --format=esm --packages=external --sourcemap --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vite": "^6.2.3",
    "esbuild": "^0.25.0",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0"
  }
}
